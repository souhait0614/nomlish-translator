{
  "name": "nomlish-translator",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "lint": "biome check",
    "format": "biome check --write",
    "test:node": "vitest --run --config ./runtime-tests/node/vitest.config.ts",
    "test:bun": "vitest --run --config ./runtime-tests/bun/vitest.config.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "vitest": "^3.0.5"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "cross-fetch": "^4.1.0",
    "fetch-cookie": "^3.1.0"
  }
}
